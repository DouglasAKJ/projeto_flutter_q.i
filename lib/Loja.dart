import 'package:flutter/material.dart';

class Loja extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Loja", style: TextStyle(fontSize: 50),),
    );
  }
}