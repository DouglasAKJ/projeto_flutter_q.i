import 'package:flutter/material.dart';

class Servicos extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Serviços", style: TextStyle(fontSize: 50),),
    );
  }
}