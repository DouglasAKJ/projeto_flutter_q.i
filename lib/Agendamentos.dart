import 'package:flutter/material.dart';

class Agendamentos extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Agendamentos", style: TextStyle(fontSize: 50),),
    );
  }
}